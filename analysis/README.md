# Archived analysis

The stage-by-stage analysis that produced the results reported in the
manuscript, preserved as run. Directory numbering follows the order of the
Methods section.

This is the provenance record, not the pipeline. To re-run the analysis on new
data use the Nextflow workflow in the repository root; to check how a published
number was generated, look here.

`README_v1.md` is the repository front page as it stood before the pipeline
rebuild.
